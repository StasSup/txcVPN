import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  pressable: {
    position: 'absolute',
    display: 'flex',
    top: '75%',
  },

  view: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 50,
    width: 100,
    height: 100,
  },

  buttonIsActive: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 50,
    width: 100,
    height: 100,
  }
});
